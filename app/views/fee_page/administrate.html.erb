<h2><%= t(@fee.name) %></h2>
<% if @fee.fee_type == Fee.fee_type_values[:user_input] %>
<%= link_to t("Add input"), controller: "fee_page", action: "add_fee_transaction", fee_id: @fee.id, transaction_type: "input", redirect: request.path %></a><br/>
  <%= link_to t("Calculate debt"), controller: "fee_page", action: "check_fee", id: @fee.id %>
<% else %>
  <%= t("Change fees") %>
<% end %>
