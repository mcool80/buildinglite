<%= form_for(@fee) do |f| %>
  <% if @fee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fee.errors.count, "error") %> prohibited this fee from being saved:</h2>

      <ul>
      <% @fee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label t("Fee") %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label t("Payment interval") %><br>
    <%= f.select :payment_interval, options_for_select([[t("Monthly"), "monthly"], [t("Yearly"), "yearly"], [t("Quarterly"), "quarterly"]]) %>
  </div>
  <div class="field">
    <%= f.label t("Currency") %><br>
    <%= f.text_field :currency %>
  </div>
  <div class="field">
    <%= f.label t("Unit") %><br>
    <%= f.text_field :unit %>
  </div>
    <%= f.hidden_field :community_id, :value => $current_community.id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
